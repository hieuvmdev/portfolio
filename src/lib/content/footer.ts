import { author } from '@/lib/content/portfolio';
import { FooterSectionType } from '@/lib/types/sections';

export const footerSection: FooterSectionType = {
  title: 'Built by Hieu Vu',
  link: `mailto:${author.email}`,
};
